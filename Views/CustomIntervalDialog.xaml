<Window x:Class="HK_AREA_SEARCH.Views.CustomIntervalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:HK_AREA_SEARCH.Views"
        mc:Ignorable="d" 
        Title="{Binding Title}" Height="300" Width="450"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height"
        MinHeight="250"
        MinWidth="400"
        FontFamily="Microsoft YaHei">
    <!-- ⭐ 添加 FontFamily="Microsoft YaHei" -->
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <DataGrid Grid.Row="0" 
                  ItemsSource="{Binding ClassItems}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  Margin="5,5,5,10"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  AlternatingRowBackground="LightGray"
                  AlternationCount="2">
            <DataGrid.Columns>
                <!-- 开始值列 -->
                <DataGridTextColumn Header="Start Value" Width="*">
                    <DataGridTextColumn.Binding>
                        <Binding Path="StartValue" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" StringFormat="F2"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                            <!-- ⭐ 表格字体 -->
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasStartValueError}" Value="True">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="HorizontalContentAlignment" Value="Right"/>
                            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>
                
                <!-- 结束值列 -->
                <DataGridTextColumn Header="End Value" Width="*">
                    <DataGridTextColumn.Binding>
                        <Binding Path="EndValue" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" StringFormat="F2"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasEndValueError}" Value="True">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="HorizontalContentAlignment" Value="Right"/>
                            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>
                
                <!-- 类别值列 -->
                <DataGridTextColumn Header="Class Value" 
                                    Binding="{Binding ClassValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                    Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="HorizontalContentAlignment" Value="Right"/>
                            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" 
                    Command="{Binding OKCommand}" 
                    Width="60" Height="25" 
                    Margin="0,0,5,0"
                    FontFamily="Microsoft YaHei"/>
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}" 
                    Width="60" Height="25"
                    FontFamily="Microsoft YaHei"/>
        </StackPanel>
    </Grid>
</Window>